---

command: gunicorn -w 5 -b 0.0.0.0:6013 wsgi
services:
  - notify-aws
  - notify-config
  - hosted-graphite
  - deskpro
instances: 1
memory: 512M
env:
  NOTIFY_APP_NAME: notify-template-preview

applications:
  - name: notify-template-preview
